1. When installing git, in "Configuration the line ending conversions" section; select the secon option
2. In "Configuration extra options" section; uncheck the all options.
3. To check if git installed correctly, open git bash and run "git version" and terminal should response your current git version.